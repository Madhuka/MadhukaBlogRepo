vjo.ctype('URIMatcher') //< public

.props({
	/**
		 * Ready state change callback when async mode is used.
		 */
		//> public type::Function
		onreadystatechangex: null	
})
.protos({
	/**
		 * Ready state change callback when async mode is used.
		 */
		//> public type::Function
		onreadystatechange: null	
})
.endType();